{% extends 'layout.twig' %}

{% block title %}MyApp / Catalog{% endblock %}
{% block content %}
    <ul>Категории товаров
        {% for cat in categories%}
            <li><a href="/catalog/{{ cat.id }}">{{ cat.title }}</a></li>
        {% endfor %}
    </ul>

{% endblock %}

