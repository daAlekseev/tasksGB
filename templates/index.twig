<!DOCTYPE html>
<html>
<head>
</head>
<body>
<table border="1px" style="border-collapse: collapse;">
    <tr>
        <th>id</th>
        <th>Title</th>
        <th>Path</th>
    </tr>
    {% for i in imgMas %}
        <tr>
            <td>{{ i.id }}</td>
            <td>{{ i.title }}</td>
            <td><a href="image.php?img={{ i.title }}"><img src="{{ i.path }}" width="200px"></a></td>
        </tr>
    {% endfor %}
</table>
</body>
</html>
